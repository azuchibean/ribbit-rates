<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Set Preferences - CurrencyEx</title>
    <link href="/stylesheets/header.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <style>
        .container {
            max-width: 600px;
            margin-top: 100px;
        }
        .form-label {
            margin-bottom: 0.5rem;
        }
        .form-select, .form-control {
            margin-bottom: 1rem;
        }
        .btn-green {
            background-color: #4CAF50;
            color: white;
            border: none;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2 class="mb-4">Set Your Preferences</h2>
        <form action="/preferences" method="post">
            <div class="mb-3">
                <label for="fromCurrency" class="form-label">From Currency:</label>
                <select class="form-select" id="fromCurrency" name="fromCurrency" required>
                    <% currencies.forEach(function(currency) { %>
                        <option value="<%= currency.code %>"><%= currency.name %> (<%= currency.code %>)</option>
                    <% }); %>
                </select>
            </div>
            <div class="mb-3">
                <label for="toCurrency" class="form-label">To Currency:</label>
                <select class="form-select" id="toCurrency" name="toCurrency" required>
                    <% currencies.forEach(function(currency) { %>
                        <option value="<%= currency.code %>"><%= currency.name %> (<%= currency.code %>)</option>
                    <% }); %>
                </select>
            </div>
            <div class="mb-3">
                <label for="threshold" class="form-label">Threshold:</label>
                <input type="number" class="form-control" id="threshold" name="threshold" step="0.0000000001" required>
            </div>
            <button type="submit" class="btn btn-green">Save</button>
        </form>
    </div>
</body>
</html>
